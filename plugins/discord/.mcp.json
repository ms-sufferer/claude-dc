{
  "mcpServers": {
    "discord": {
      "command": "uvx",
      "args": ["--from", "${CLAUDE_PLUGIN_ROOT}/server", "mcp-discord"],
      "env": {
        "DISCORD_TOKEN": "${DISCORD_TOKEN}"
      }
    }
  }
}
